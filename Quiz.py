import random
import string


for k in range(0, 11):
    number = random.randint(0, 1)
    if number == 0:

def caeser():
    number = random.randint(0, 25)
    generateQuestion = random.randint(0, 1)
    alphabetList = list(string.ascii_uppercase)
    if generateQuestion == 0:

